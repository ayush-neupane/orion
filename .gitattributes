# Normalize line endings: LF in the repo for all text files,
# regardless of contributor OS (Windows/macOS/Linux).
* text=auto

# Windows-specific files keep CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf

# Binary assets
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
